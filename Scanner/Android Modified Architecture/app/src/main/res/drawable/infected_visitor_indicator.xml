<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Shadow -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="10dp"/>
            <gradient android:angle="90"
                android:startColor="@color/colorShadowStart"
                android:endColor="@color/colorShadowEnd"/>
        </shape>
    </item>

    <!-- Border -->
    <item android:bottom="5dp">
        <shape android:shape="rectangle">
            <corners android:radius="10dp"/>
            <solid android:color="@color/failureIndicatorBorder"/>
        </shape>
    </item>

    <!-- Background -->
    <item android:top="2dp" android:bottom="7dp" android:left="2dp" android:right="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="10dp"/>
            <solid android:color="@color/failureIndicator" />
        </shape>
    </item>

    <!-- Foreground -->
    <item
        android:drawable="@drawable/ic_warning"
        android:top="20dp"
        android:bottom="20dp"
        android:left="20dp"
        android:right="20dp" />

</layer-list>